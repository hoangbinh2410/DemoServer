from django.urls import path
from demoapi import views
from django.conf import settings

# app_name = 'api'
# urlpatterns = [
#     path('/getlistteacher', views.Getlistteach.as_view(), name='listteacher'),
#     path('/postinforstudent',views.ListStuden.as_view(),name = 'liststudent'),
# ]